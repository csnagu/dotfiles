#alias cat='bat'
alias ls='ls -G'
alias awk='gawk'
alias grep='ggrep'
alias sed='gsed'
alias vim='nvim'

# commit shortcut
ink() {
  cd $HOME/ink || return 1
  git add -A
  git diff --cached --quiet && { echo "no changes"; return 0; }
  git commit -m "ink: $(date '+%Y-%m-%d')"
  git push
}

